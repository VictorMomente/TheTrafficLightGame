Para jogar deve ter o python 3 e o Pygame instalados.

Baixar o python-pip:
    sudo apt install python3-pip

Baixar o Pygame:
    sudo pip3 install pygame

Após isso, abrir o terminal no diretório e escrever o comando "make"
